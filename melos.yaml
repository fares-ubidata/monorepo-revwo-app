name : monorepo_revwo_app

packages:
  - packages/*
  - apps/**

scripts:
  get : 
      run : "melos bootstrap"
      
  build:clean:
    run: melos clean &&
         melos bootstrap 
     
    description: Run all build targets from a clean state.   